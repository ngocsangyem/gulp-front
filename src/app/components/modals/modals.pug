mixin modals(data)
	- data = data || {}
	- data.class = data.class || ''
	- data.content = data.content || 'Some content here'

	.modals(class=data.class)&attributes(attributes)
		if block
			block
		else
			!= data.content