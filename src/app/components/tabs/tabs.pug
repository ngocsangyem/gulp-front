mixin tabs(data)
	- data = data || {}
	- data.class = data.class || ''
	- data.content = data.content || 'Some content here'

	.tabs(class=data.class)&attributes(attributes)
		if block
			block
		else
			!= data.content