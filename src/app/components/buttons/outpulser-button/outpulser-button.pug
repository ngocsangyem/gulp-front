mixin outpulser-button(data)
	- data = data || {}
	- data.class = data.class || ''
	- data.content = data.content || 'Gulp Front'

	button.outpulser-button(class=data.class, role="button", type="button")&attributes(attributes)
		if block
			block
		else
			!= data.content
