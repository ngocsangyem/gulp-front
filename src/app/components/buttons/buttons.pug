mixin buttons(data)
	- data = data || {}
	- data.class = data.class || ''
	- data.content = data.content || 'Some content here'

	.buttons(class=data.class)&attributes(attributes)
		if block
			block
		else
			!= data.content