mixin buttonsanimated-close-button(data)
	- data = data || {}
	- data.class = data.class || ''
	- data.content = data.content || 'Some content here'

	.buttonsanimated-close-button(class=data.class)&attributes(attributes)
		if block
			block
		else
			!= data.content