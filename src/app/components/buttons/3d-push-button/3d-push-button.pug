mixin buttons3d-push-button(data)
	- data = data || {}
	- data.class = data.class || ''
	- data.content = data.content || 'Some content here'

	.buttons3d-push-button(class=data.class)&attributes(attributes)
		if block
			block
		else
			!= data.content