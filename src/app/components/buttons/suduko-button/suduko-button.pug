mixin buttonssuduko-button(data)
	- data = data || {}
	- data.class = data.class || ''
	- data.content = data.content || 'Some content here'

	.buttonssuduko-button(class=data.class)&attributes(attributes)
		if block
			block
		else
			!= data.content