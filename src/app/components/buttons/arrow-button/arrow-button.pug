mixin buttonsarrow-button(data)
	- data = data || {}
	- data.class = data.class || ''
	- data.content = data.content || 'Some content here'

	.buttonsarrow-button(class=data.class)&attributes(attributes)
		if block
			block
		else
			!= data.content