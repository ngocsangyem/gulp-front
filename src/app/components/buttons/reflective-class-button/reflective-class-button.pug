mixin buttonsreflective-class-button(data)
	- data = data || {}
	- data.class = data.class || ''
	- data.content = data.content || 'Some content here'

	.buttonsreflective-class-button(class=data.class)&attributes(attributes)
		if block
			block
		else
			!= data.content