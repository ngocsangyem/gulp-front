mixin buttonsrotating-cuboid-button(data)
	- data = data || {}
	- data.class = data.class || ''
	- data.content = data.content || 'Some content here'

	.buttonsrotating-cuboid-button(class=data.class)&attributes(attributes)
		if block
			block
		else
			!= data.content